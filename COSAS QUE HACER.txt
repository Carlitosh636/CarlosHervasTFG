-Estilos:
    -Hacer el codegen más ALTO Y ANCHO, para que tenga más espacio. Los botones se podrían poner justo debajo del enunciado para aprovechar espacio.
    -En el menú principal, plantearse en vez de un GridPane tener un VBox de todos los botones y que sea ancho, para que entre todo el texto, incluso una descripción.
-Feats:
    -EL DIAGRAMA EXTRA PARA EL CASO RECURSIVO QUE DEPENDA DEL INPUT (ej: calculo potencia si descomp es //2).
    Mirar la opción de crear siempre 2 diagramas, pero el segundo está siempre oculto, y se muestra/actualiza únicamente si
    tenemos un caso donde el caso recursivo dependa de los params.
    -Los casos base del codegen hay que cambiarlos.
    -mirar si los builders se pueden refactorizar para solo tener 2 (diagrama simple y multiple), que cada instancia maneje el parsing del input en vez del builder.
    -más problemas (contains digit en proceso, luego hacer list contains element y selection sort)


