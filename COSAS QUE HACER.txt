-Estilos:
    -Textos más grandes (excepto botones, ya están hechos)
-Feats:
    -EL DIAGRAMA EXTRA PARA EL CASO RECURSIVO QUE DEPENDA DEL INPUT (ej: calculo potencia si descomp es //2).
        -Detectar cuando ocurre ese caso, y rellenarlo con lo necesario en tal
    -Los casos base del codegen hay que cambiarlos.
    -mirar si los builders se pueden refactorizar para solo tener 2 (diagrama simple y multiple), que cada instancia maneje el parsing del input en vez del builder.
    -más problemas (list contains element y selection sort, entre otros)
-Memoria:
    - Hacer más subapartados (coger lo que ya hay e ir haciendo subapartados)
    - Reescribir algunas partes, los que ahora pasarán a ser apartados.



