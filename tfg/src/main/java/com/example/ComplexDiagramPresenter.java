package com.example;

public class ComplexDiagramPresenter extends DiagramPresenter {
    //esto tiene que tener 2 labels para el display de las soluciones

    public ComplexDiagramPresenter(Diagram model) {
        super(model);
    }
}
